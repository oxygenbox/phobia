const resolvedSlotValue = require(`./resolvedSlotValue`);
const scrambledNumberArray = require(`./scrambledNumberArray`);
const makeArraySpeakable = require(`./makeArraySpeakable`);
const mixupArrayItems = require(`./mixupArrayItems`);
const lastArrayItem = require(`./lastArrayItem`)


module.exports = {
    resolvedSlotValue: resolvedSlotValue,
    scrambledNumberArray: scrambledNumberArray,
    makeArraySpeakable: makeArraySpeakable,
    mixupArrayItems: mixupArrayItems,
    lastArrayItem:lastArrayItem
    
}